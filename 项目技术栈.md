> **基线规范**：采用 OpenJDK 17 + Spring Boot 3.x.x，保持和开题报告的一致性，同时利用 Java 17 的稳定性与新特性。

### 最小可行配置（MVP/交付优先）
- **后端**：Spring Boot + MyBatis-Plus + JWT/Spring Security + MySQL + Redis。先做单体，不拆微服务；用 SpringDoc 自动出接口，后续再加 Kafka/Quartz 也能插值。
- **前端**：React + Vite + Ant Design + Axios + 高德地图 JS API。页面按功能拆模块，先实现核心列表/报名/签到/异常，避免一开始做“全套社交”。
- **部署**：Docker 镜像 + Nginx 静态 + MySQL/Redis，配合简单的 shell 脚本或 GitHub Actions 触发，保持一台服务器即可。监控用 Prometheus/Grafana 捕地图或签到异常即可；后续复杂化再加。
- **交付节奏**：保持一个 release branch，优先完成活动管理 + 报名 + 签到 + 轨迹 + 反馈 5 条闭环，再扩展推荐、评价、通知。错峰交付，避免一次性上线太多模块。

