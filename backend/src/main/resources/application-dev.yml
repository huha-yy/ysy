# ==========================================
# 开发环境配置
# ==========================================

spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/outdoor_hiking?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123666888

  # Redis 配置（暂时不使用）
  # data:
  #   redis:
  #     host: localhost
  #     port: 6379
  #     database: 0
  #     password: 
  #     timeout: 5000ms
  #     lettuce:
  #       pool:
  #         max-active: 8
  #         max-idle: 8
  #         min-idle: 0
  #         max-wait: -1ms

# MyBatis Plus 开发环境配置
mybatis-plus:
  configuration:
    # 开启SQL日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 日志级别 - 开发环境
logging:
  level:
    root: INFO
    com.hiking: DEBUG
    com.hiking.mapper: DEBUG

