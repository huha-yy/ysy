## 户外徒步活动管理系统：业务流程与核心用例

### 主要角色
- 参与者：报名/签到/反馈  
- 组织者：发布活动/审核报名/监控执行  
- 系统管理员：合规审核、轨迹监控、通知与审计

### 业务流程
1. **参与者流程**
   - 注册/登录后浏览 `activities` 与 `routes.points_info`，参考 `requirement_info` 与附件判断是否满足报名条件。  
   - 提交报名写入 `registrations`（记下 `qualification_info`），后台生成 `system_events` 通知结果。  
   - 根据组织者发的 `system_events` 提前做好准备；在集合点/关键点/终点依次提交 `checkin_records`，`point_index` 与 `routes.points_info` 联动判断偏离或延迟。  
   - 活动结束后通过 `activity_feedback` 给出评分与评价，触发推荐与排名更新（`activity_stats`）。

2. **组织者流程**
   - 在 `activities` 中创建活动，`requirement_info`/`attachments` 记录审核条件、路线文档与健康材料。  
   - 审核报名、更新 `registrations.status`，并在 `system_events` 里发通知（`category=review_result`）与审计日志。  
   - 实时监控 `checkin_records` 与 `trajectory_events`，遇到异常写入 `trajectory_events` 并通知参与者/管理员。  
   - 任务结束后汇总 `activity_feedback` 与 `activity_stats`，评估热度、完成率与异常率，指导复盘与优化。

3. **管理员流程**
   - 审核活动与组织者资质，控制 `activities.status`；配合 `system_events` 记录审批操作。  
   - 监控 `trajectory_events`、异常处理与舆情，通过 `status` 变更表示事件已处理，并在 `activity_stats` 中统计异常数量。  
   - 统一管理通知/审计：`system_events.event_type` 可带 `notification` 或 `audit`，用于提醒参与者或备查。

### 核心用例
| 场景 | 步骤 | 涉及表 |
| --- | --- | --- |
| 报名审核 | 组织者筛选 `registrations.status=pending`，校验 `qualification_info`，更新状态并写入 `system_events` 通知参与者 | registrations、system_events |
| 签到与预警 | 参与者提交 `checkin_records`，系统结合 `routes.points_info` 计算距离/时间，偏离时生成 `trajectory_events` | checkin_records、routes、trajectory_events |
| 异常事件处理 | 管理员/组织者将 `trajectory_events.status` 设为 `resolved`，记录 `handled_by` 与说明，通知受影响用户 | trajectory_events、system_events |
| 活动复盘 | 根据 `activity_feedback` 与 `trajectory_events` 统计，更新 `activity_stats`（热度/完成率/异常数），用于排行榜和合规 | activity_feedback、trajectory_events、activity_stats |

> 以上文档建议同步到项目文档库，每次需求调整后及时更新，保持流程记录与实际实现一致。

